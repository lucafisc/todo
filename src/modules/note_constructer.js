//note constructor
class Book {
    constructor(title, description, tags, flagged, dueDate, project, done) {
      this.title = title;
      this.description = description;
      this.tags = tags;
      this.flagged = flagged;
      this.dueDate = dueDate;
      this.project = project;
      this.done = done;
    }
  }


