//note constructor
class Book {
    constructor(title, description, tags, flagged, dueDate, project) {
      this.title = title;
      this.description = description;
      this.tags = tags;
      this.flagged = flagged;
      this.dueDate = dueDate;
      this.project = project;

    }
  }


